Run final_preprocess.py

In load_covidqa_dic.py, you can select relation type i.e. cuurently we are using, relation_list=['uses','treats','prevents','isa','diagnoses','co-occurs_with','associated_with','affects']

For n_hops 
In preproess.py, under sorted_triple function we can pass value of n inside n_hops. 